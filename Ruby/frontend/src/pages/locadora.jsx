import React from "react";

const Locadora = () => {
    const style = {
        height: "100vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontFamily: 'system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial',
        fontSize: "1.5rem",
        color: "#222",
        background: "#f8f9fa",
    };

    return <main style={style}>hello woelds</main>;
};

export default Locadora;